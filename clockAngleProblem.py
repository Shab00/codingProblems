from typing import List

def solve(target_angle: int) -> List:
    H = ((5.5) *)

inputs = [(120,[4, 0]), (200,[2, 40])]

for i, expected in inputs:
    result = solve(i)
    print(f"Inputs: {i} | Output: {result} | Expected: {expected}")
